'use client';

/**
 * BottomNav Component
 *
 * Adaptive navigation component for mobile devices (phone breakpoint)
 * Shows primary navigation items in a bottom navigation bar
 *
 * TODO: Implementation in future task
 * - Import navigation sections from navigation-config.ts
 * - Render navigation items with icons
 * - Handle active state
 * - Support device class detection via useDeviceClass hook
 */

export default function BottomNav() {
  return null;
}
