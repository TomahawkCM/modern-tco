'use client';

/**
 * NavRail Component
 *
 * Adaptive navigation component for tablet devices
 * Shows vertical navigation rail on the left side
 *
 * TODO: Implementation in future task
 * - Import navigation sections from navigation-config.ts
 * - Render primary items in collapsed state
 * - Support expand/collapse functionality
 * - Handle active state
 * - Support device class detection via useDeviceClass hook
 */

export default function NavRail() {
  return null;
}
