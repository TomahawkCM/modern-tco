/**
 * Server — Server-side orchestration logic
 *
 * Allowed:
 * - Supabase queries
 * - Stripe validation
 * - AI request orchestration
 * - Sync jobs
 *
 * NOT allowed:
 * - Core financial math (must live in /engine)
 */
