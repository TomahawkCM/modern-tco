/**
 * Lib — Shared utilities and helpers
 *
 * Allowed:
 * - Date helpers
 * - Formatting helpers
 * - Validation (Zod schemas)
 * - Constants
 *
 * NOT allowed:
 * - Financial engine logic (must live in /engine)
 * - Supabase queries
 */
